server:
  port: 8608

spring:
  application:
    name: mogu-spider
  cloud:
    nacos:
      username: nacos
      password: nacos

      discovery:
        server-addr: http://111.230.32.175:8848
        namespace: dev
      config:
        server-addr: http://111.230.32.175:8848
        file-extension: yaml
        #指定分组
        group: dev
        #指定命名空间
        namespace: dev
