server:
  port: 8606

spring:
  application:
    name: mogu-monitor
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: http://111.230.32.175:8848
        namespace: dev
      config:
        server-addr: http://111.230.32.175:8848
        file-extension: yaml
        #指定分组
        group: dev
        #指定命名空间
        namespace: dev
